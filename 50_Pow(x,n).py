"""
    50. Pow(x, n)
    Implement pow(x, n), which calculates x raised to the power n (i.e., x^n).

    :type x: float
    :type n: int
    :rtype: float

"""
def myPow(self, x, n):
    return(pow(x,n))

print(myPow("",2.10000,3))
